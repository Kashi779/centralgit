---
- name: Install and start nginx on dbserver
  hosts: webserver1 
  become: true
  tasks:

    - name: Stop and enable nginx service
      systemd:
        name: nginx
        state: stopped
        enabled: true

    - name: unistall nginx
      yum:
        name: nginx
        state: absent 

